{
  "$schema": "https://schemas.adia.ai/skill/v1.json",
  "name": "perf-verifier",
  "version": "0.2.0",
  "description": "Reason about perceived latency — the gap between an action and the user's belief that it worked — and emit the invariants every async surface must satisfy — response latency windows, skeleton vs spinner vs optimistic decisions, streaming UX, layout-stability budgets, and feedback obligations. Use when loading UX feels slow despite acceptable wire latency, when CLS is eroding trust, or when streaming responses need coherent presentation.",
  "status": "stable",
  "authors": [
    "kim"
  ],
  "tags": [
    "constraint",
    "typed-pipeline"
  ],
  "files": [
    "CHANGELOG.md",
    "ROADMAP.md",
    "SKILL.md",
    "cancellation/contract.json",
    "cls/budget.json",
    "decisions/skeleton-vs-spinner.json",
    "optimistic/eligibility.json",
    "skill.json",
    "streaming/posture.json",
    "thresholds/perception.json"
  ],
  "domain": "ui-design",
  "interface": {
    "input": {
      "description": "Relevant schemas (UISchema, BrandSchema, InteractionSchema) and emitted artifacts."
    },
    "output": {
      "description": "PerceivedPerformanceProof — constraint-satisfaction proof or violation report."
    },
    "config": {
      "description": "Thresholds for pass/warn/fail; remediation policy."
    }
  },
  "invariants": [
    {
      "id": "INV-PER-001",
      "description": "Every proof cites specific schema paths or CSS rules it evaluates",
      "enforcement": "convention"
    },
    {
      "id": "INV-PER-002",
      "description": "Remediation suggestions are scoped to the schema/artifact that can fix them",
      "enforcement": "convention"
    }
  ],
  "composition": {
    "upstream": [
      "ui-orchestrator",
      "ui-schema-brand",
      "ui-schema-ui",
      "ui-schema-interaction",
      "ui-build-tokens"
    ],
    "downstream": [
      "ui-audit-quality"
    ],
    "peer": []
  },
  "target": "agent"
}