{
  "name": "regex-decomposer",
  "version": "0.1.0",
  "status": "draft",
  "target": "both",
  "description": "Decompose, design, and grade a regular expression on two crossing axes — LANGUAGE (target-set → anchoring → classes/quantifiers → groups → flags/dialect) and MATCH (compiles → examples → safety → robustness → readability) — scored separately, with MATCH routed to a self-tested checker that compiles the pattern, runs the example set under the full/partial mode, and statically scans for catastrophic backtracking, and the 'wrong language' failure routed to a fresh-context adversarial counter-example hunt. Backed by a gated rubric and a pattern-spec card {pattern, flags, engine, positives[], negatives[]}.",
  "keywords": ["regex", "regular-expression", "redos", "catastrophic-backtracking", "pattern-matching", "anchoring", "target-set", "example-set", "dialects", "pcre", "re2", "decomposition", "adversarial-verification", "definition-of-done"],
  "files": [
    "SKILL.md",
    "references/decomposition-method.md",
    "references/language-axis.md",
    "references/match-axis.md",
    "references/redos-and-safety.md",
    "references/dialects.md",
    "references/policy.md",
    "bin/regex-check.py",
    "regex-decomposer.corpus.json",
    "CHANGELOG.md",
    "ROADMAP.md"
  ]
}
