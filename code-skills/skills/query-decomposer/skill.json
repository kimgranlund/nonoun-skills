{
  "name": "query-decomposer",
  "version": "0.2.2",
  "status": "beta",
  "target": "both",
  "description": "Decompose, design, and grade a SQL query on two crossing axes — SEMANTICS (question → grain → joins/filters → aggregation → fit) and EXECUTION (parses → binds → plan/run → performance → safety) — scored separately, with EXECUTION routed to the real EXPLAIN/dry-run (a self-tested harness) and the wrong-grain / fan-out failure routed to a static SQL smell linter + a row-count/uniqueness grain check. Backed by a gated rubric, a query-spec card + plan-report protocol, and dialect playbooks (Postgres/MySQL/SQLite/BigQuery/Snowflake). NOT for a unit of application code — a function or implementation graded on its spec (code-decomposer); a regular expression / regex pattern (regex-decomposer); or schema design (arch-system).",
  "keywords": ["sql", "query-review", "grain", "join-fan-out", "wrong-grain", "explain-plan", "cartesian", "group-by", "window-functions", "sargable", "dialects", "definition-of-done", "decomposition", "query-rubric", "analytics"],
  "files": [
    "SKILL.md",
    "references/decomposition-method.md",
    "references/semantics-axis.md",
    "references/execution-axis.md",
    "references/grain-and-joins.md",
    "references/dialects.md",
    "references/policy.md",
    "bin/query-harness.py",
    "bin/sql-lint.py",
    "examples/walkthrough.md",
    "examples/active-roles.red.sql",
    "examples/active-roles.green.sql",
    "query-decomposer.corpus.json",
    "CHANGELOG.md",
    "ROADMAP.md"
  ]
}
